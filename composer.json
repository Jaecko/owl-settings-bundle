{
    "name": "owl-concept/settings-bundle",
    "description": "Bundle Symfony pour le paramétrage global de l'application et les préférences utilisateur (thème, langue, etc.) avec interface auto-générée | Symfony bundle for global application settings and per-user preferences with auto-generated UI",
    "type": "symfony-bundle",
    "license": "proprietary",
    "require": {
        "php": ">=8.1",
        "symfony/framework-bundle": "^6.4 || ^7.0",
        "symfony/twig-bundle": "^6.4 || ^7.0",
        "symfony/security-bundle": "^6.4 || ^7.0",
        "symfony/cache": "^6.4 || ^7.0",
        "doctrine/dbal": "^3.6 || ^4.0",
        "twig/twig": "^3.0"
    },
    "autoload": {
        "psr-4": {
            "OwlConcept\\SettingsBundle\\": "src/"
        }
    },
    "extra": {
        "symfony": {
            "require": "^6.4 || ^7.0"
        }
    }
}
